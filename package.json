{
  "name": "dynamicboardsonline",
  "version": "1.0.0",
  "private": true,
  "description": "a website to buy all types of electric longboards.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp serve",
    "build": "gulp sass"
  },
  "devDependencies": {
    "browser-sync": "^2.24.7",
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1"
  },
  "dependencies": {
    "popper.js": "^1.14.5"
  }
}
